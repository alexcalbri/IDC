# Hostpot Migrations

Place hostpot database migrations here. Migration scripts should be versioned, checksummed by the migration runner and applied only to tenant databases where the module is installed.
